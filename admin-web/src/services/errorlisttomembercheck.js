export default{

    passVal(getArr){

        try{
        console.log('now in JS - getARR')
        console.log(getArr)
        const myArrStr = {
            
        };
        localStorage.setItem('passVal_X', myArrStr);
        } catch (error){
            console.error(error)
        }

       
    },
    // checkVal(getID){


    // }
}